---
- block:
  - debug:
      msg: CONFIRM WHETHER WE NEED TO CAUSE ANSIBLE TO FAIL TO RERUN WITH UPDATES
# We should assert that everything has worked
#  - assert:
#    that:
#      - "worker.spark-worker.state.running"
#      - "master.spark-master.state.running"
  tags:
    - system_tests
    - all_tests
